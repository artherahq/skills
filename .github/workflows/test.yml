name: test

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  catalog-integrity:
    name: marketplace and skill integrity
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependency
        run: pip install --quiet pytest

      - name: Verify lock file
        run: python scripts/build_skill_lock.py --check

      # The lock is derived from marketplace.json, so it cannot see a skill that
      # was added to disk and never registered — that skill just silently ships
      # to no one. This catches it, plus missing policies, descriptors, and
      # README rows.
      - name: Validate catalog consistency
        run: python scripts/validate_catalog.py

      - name: Test lock builder
        working-directory: scripts
        run: pytest test_build_skill_lock.py -q

      - name: Test catalog validator
        working-directory: scripts
        run: pytest test_validate_catalog.py -q

  point-in-time-research:
    name: point-in-time-research harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/point-in-time-research/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install --quiet pandas numpy pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning information_set_compare.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_information_set_compare.py -q

  compliance-audit-trail:
    name: compliance-audit-trail harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/compliance-audit-trail/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependency
        run: pip install --quiet pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning governance_manifest_gate.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_governance_manifest_gate.py -q

  multiple-testing-correction:
    name: multiple-testing-correction harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/multiple-testing-correction/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install --quiet numpy scipy pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning multiplicity_gate.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_multiplicity_gate.py -q

  gamma-exposure:
    name: gamma-exposure harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/gamma-exposure/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependency
        run: pip install --quiet pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning gex_gate.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_gex_gate.py -q

  equity-research-report:
    name: equity research report gates
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/equity-research-report/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependency
        run: pip install --quiet pytest

      - name: Regression tests
        run: pytest test_validate_equity_report.py -q

      - name: Invalid manifest fails closed
        run: |
          if python validate_equity_report.py /dev/null; then
            echo "validator unexpectedly accepted an empty manifest"
            exit 1
          fi

  backtest-validation:
    name: backtest-validation harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/backtest-validation/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install --quiet pandas numpy pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning validation_gauntlet.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_validation_gauntlet.py -q

  execution-position:
    name: execution-position harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/execution-position/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependency
        run: pip install --quiet pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning position_gate.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_position_gate.py -q

  factor-research:
    name: factor-research harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/factor-research/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install --quiet pandas numpy pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning factor_evaluate.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_factor_evaluate.py -q

  portfolio-optimization:
    name: portfolio-optimization harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/portfolio-optimization/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install --quiet pandas numpy pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning optimize_portfolio.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_optimize_portfolio.py -q

  risk-assessment:
    name: risk-assessment harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/risk-assessment/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install --quiet pandas numpy pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning risk_profile.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_risk_profile.py -q

  strategy-generation:
    name: strategy-generation harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/strategy-generation/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependency
        run: pip install --quiet pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning validate_strategy_spec.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_validate_strategy_spec.py -q

  industry-design-direction:
    name: industry-design-direction harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/industry-design-direction/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependency
        run: pip install --quiet pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning verify_contrast.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_verify_contrast.py -q

  ui-design-system:
    name: ui-design-system harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/ui-design-system/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependency
        run: pip install --quiet pytest

      - name: Smoke — both harnesses run their self-contained demo
        run: |
          python design_tokens.py --demo > /dev/null
          python design_lint.py --demo > /dev/null

      - name: Regression tests
        run: pytest -q

  minimal-editorial-exports:
    name: minimal-editorial-exports harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/minimal-editorial-exports/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependency
        run: pip install --quiet pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning exports_gate.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_exports_gate.py -q

  minimal-editorial-poster:
    name: minimal-editorial-poster harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/minimal-editorial-poster/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependency
        run: pip install --quiet pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning poster_gate.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_poster_gate.py -q

  ui-asset-sourcing:
    name: ui-asset-sourcing harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/ui-asset-sourcing/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependency
        run: pip install --quiet pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning asset_lint.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_asset_lint.py -q

  trading-ui-patterns:
    name: trading-ui-patterns harness
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/trading-ui-patterns/scripts
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependency
        run: pip install --quiet pytest

      - name: Smoke — the bundled harness runs and emits no warnings
        run: |
          python -W error::RuntimeWarning pattern_audit.py --demo > /dev/null

      - name: Regression tests
        run: pytest test_pattern_audit.py -q
