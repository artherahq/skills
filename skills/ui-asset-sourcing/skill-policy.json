{
  "schema_version": "aria.skill-policy.v1",
  "allowed_tools": ["read_file", "list_directory", "write_file"],
  "permissions": ["read-only", "network", "workspace-write"],
  "scripts": {
    "execution": "approval",
    "network": true,
    "workspace_write": true
  }
}
